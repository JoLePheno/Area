package action

import (
	"DEV_area_2018/server/models"
)

func FriendBirthday(area models.Area, user models.User, service models.Service) {
	/*_, body, err := gorequest.New().Get("https://graph.facebook.com/me/friends?fields=birthday,name&access_token=" + service.AccessToken).End()
	if err != nil {
		return
	}*/
}